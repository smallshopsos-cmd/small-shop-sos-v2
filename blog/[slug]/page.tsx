import type { Metadata } from "next";
import { notFound } from "next/navigation";
import { SiteFooter, SiteHeader } from "../../site-chrome";
import { getPost, posts } from "../posts";
export function generateStaticParams(){return posts.map(({slug})=>({slug}));}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const post=getPost(slug);return post?{title:`${post.title} | Small Shop SOS`,description:post.description,alternates:{canonical:`/blog/${post.slug}/`}}:{};}
export default async function BlogPostPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const post=getPost(slug);if(!post)notFound();return <main className="route-page post-page"><SiteHeader/><style>{`.post-page .post-hero{padding:clamp(56px,9vw,118px) clamp(24px,9vw,135px);background:#1a1a1f;color:#f6f3ef}.post-page .post-hero>div,.post-page article{max-width:900px;margin:0 auto}.post-page h1{max-width:900px;margin:8px 0 22px;font:700 clamp(49px,7vw,95px)/.91 var(--display);letter-spacing:-.065em}.post-page .post-meta{color:#e3bccf;font-size:10px;font-weight:800;letter-spacing:.07em;text-transform:uppercase}.post-page .lead{max-width:700px;color:#e8e0e4;font:400 clamp(20px,2.6vw,29px)/1.36 var(--display)}.post-page article{padding:clamp(50px,8vw,100px) clamp(24px,7vw,80px)}.post-page article h2{margin:48px 0 14px;font:700 clamp(32px,4vw,53px)/.96 var(--display);letter-spacing:-.05em}.post-page article p{max-width:690px;font-size:17px;line-height:1.75}.post-page .post-next{padding:clamp(45px,7vw,85px) clamp(24px,9vw,135px);background:#eee8e6}.post-page .post-next>div{max-width:900px;margin:0 auto}.post-page .post-next h2{max-width:720px;margin:8px 0 17px;font:700 clamp(38px,5vw,64px)/.93 var(--display);letter-spacing:-.06em}`}</style><section className="post-hero"><div><a className="back-link" href="/blog/" style={{color:"#e4b8cc"}}>← All useful bits</a><p className="eyebrow" style={{color:"#e4b8cc"}}>{post.eyebrow}</p><h1>{post.title}</h1><p className="post-meta">{post.date} · {post.readingTime}</p><p className="lead">{post.intro}</p></div></section><article>{post.sections.map(section=><section key={section.heading}><h2>{section.heading}</h2>{section.paragraphs.map(paragraph=><p key={paragraph}>{paragraph}</p>)}</section>)}</article><section className="post-next"><div><p className="eyebrow">GOT ONE OF THESE?</p><h2>Tell me what needs sorting.</h2><p>You do not need a perfectly formed plan before you get in touch.</p><a className="cta" href="/work-with-me/">Work with me →</a></div></section><SiteFooter/></main>}
